use bevy::prelude::*;

pub struct CraftMenuPlugin;

// Figure out how to decouple crafting UI from the Action of crafting ... am I thinking too much abt it ?
impl Plugin for CraftMenuPlugin {
    fn build(&self, app: &mut App) {
        app
            ;
    }
}